/**
 * @file src\server\irc\SslSocketLayer.cpp
 *
 * @brief Implements the ssl socket layer class. Not yet implemented due to ACE+SSL issues on Windows
 */

#include "SslSocketLayer.h"

//#include <ace/SSL/SSL_SOCK_Stream.h>

/**
 * @fn bool SslSocketLayer::Connect(char const* hostname, uint16 port)
 *
 * @brief Connects.
 *
 * @author Machiavelli
 * @date 9-2-2013
 *
 * @param hostname The hostname.
 * @param port     The port.
 *
 * @return true if it succeeds, false if it fails.
 */

bool SslSocketLayer::Connect(char const* hostname, uint16 port)
{
return false;
}